# Floating upstream series tags. Record resolved digests for reference measurements.
# Deliberate stability gate: EXPECTED_IMAGES in tests/repository-contract.py
# mirrors these entries, so update both files in the same commit.
PYTHON_IMAGE=python:3.14-slim
NODE_IMAGE=node:24-alpine
NGINX_IMAGE=nginx:1.30-alpine
REGISTRY_IMAGE=registry:3
